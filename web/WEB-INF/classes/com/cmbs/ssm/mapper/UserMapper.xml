<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cmbs.ssm.mapper.UserMapper">
    <insert id="addUser" parameterType="com.cmbs.ssm.pojo.User" >
        INSERT INTO tuser VALUES(NULL,#{mobileNumber},#{roamingStatus},#{comLevel},#{customerId},#{accountId});
    </insert>
</mapper>